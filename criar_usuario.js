document.addEventListener('DOMContentLoaded',()=>{Ecos.shell('Criar Usuário','painel');Ecos.footer();const user=Ecos.requireAdmin();if(!user)return;document.getElementById('app').innerHTML=`<div class="page"><div class="title"><div class="kicker">MESTRE</div><h2>Criar Usuário</h2><p>Adicione um novo Jogador (ou Mestre) ao sistema.</p></div><div class="panel" style="max-width:480px"><p id="error" class="erro" hidden></p><p id="ok" class="ok" hidden></p><form id="criarUsuarioForm"><label>Usuário</label><input id="usuario" required><label>Senha</label><input id="senha" type="password" required><label>Tipo</label><select id="tipo"><option value="player">Jogador</option><option value="admin">Mestre</option></select><div class="actions"><button class="btn" type="submit">CRIAR</button></div></form></div><div class="actions"><a class="mini" href="painel.html">Voltar</a></div></div>`;document.getElementById('criarUsuarioForm').addEventListener('submit',async e=>{e.preventDefault();const username=document.getElementById('usuario').value.trim(),pass=document.getElementById('senha').value,type=document.getElementById('tipo').value,err=document.getElementById('error'),ok=document.getElementById('ok');err.hidden=true;ok.hidden=true;if(!username||!pass){err.textContent='Preencha usuário e senha.';err.hidden=false;return;}const db=Ecos.loadDB();if(db.usuarios.some(u=>u.usuario.toLowerCase()===username.toLowerCase())){err.textContent='Usuário já existe.';err.hidden=false;return;}db.usuarios.push({id:Ecos.nextUserId(db),usuario:username,senha:await Ecos.hashPassword(pass),tipo});Ecos.saveDB(db);ok.textContent=`Usuário "${username}" criado com sucesso.`;ok.hidden=false;e.target.reset();});});
